package main

func maximumSumOfHeights(maxHeights []int) int64 {
	n := len(maxHeights)
	left, right := make([]int, n), make([]int, n)
	for i := 0; i < n; i++ {
		left[i], right[i] = -1, -1
	}

	for i := 0; i < n; i++ {

	}
}
